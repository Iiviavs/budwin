<Window x:Class="Budwin.UI.FlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Budwin.UI.Controls"
        Title="Budwin"
        Width="380"
        Height="560"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif">

    <Window.Resources>
        <!-- Fluent Dark Colors -->
        <SolidColorBrush x:Key="CardBackground" Color="#242424" />
        <SolidColorBrush x:Key="CardBorder" Color="#383838" />
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF" />
        <SolidColorBrush x:Key="TextSecondary" Color="#9E9E9E" />
        <SolidColorBrush x:Key="TextMuted" Color="#757575" />
        
        <SolidColorBrush x:Key="CpuAccent" Color="#00A2ED" />
        <SolidColorBrush x:Key="GpuAccent" Color="#76B900" />
        <SolidColorBrush x:Key="RamAccent" Color="#9B59B6" />
        <SolidColorBrush x:Key="NetAccent" Color="#00D2D3" />

        <!-- Modern Button Style -->
        <Style x:Key="FluentButton" TargetType="Button">
            <Setter Property="Background" Value="#333333" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#222222" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource FluentButton}">
            <Setter Property="Background" Value="#3D1E24" />
            <Setter Property="Foreground" Value="#FF6B6B" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E74C3C" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Main Outer Border with Drop Shadow and Mica Dark Glass effect -->
    <Border Background="#1A1A1A" BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.6" Color="#000000" />
        </Border.Effect>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="Auto" /> <!-- CPU & GPU Cards -->
                <RowDefinition Height="Auto" /> <!-- RAM & Net Cards -->
                <RowDefinition Height="*" />    <!-- Process Leaderboard -->
                <RowDefinition Height="Auto" /> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <Grid Grid.Row="0" Margin="0,0,0,14">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚡" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBlock Text="budwin" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" />
                    <Border Background="#2E3A4E" CornerRadius="4" Margin="8,0,0,0" Padding="5,1">
                        <TextBlock Text="v1.0.0" FontSize="10" FontWeight="Bold" Foreground="#4A90E2" />
                    </Border>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="BtnClose" Content="✕" Width="24" Height="24" Style="{StaticResource FluentButton}" Click="BtnClose_Click" ToolTip="Close Flyout" />
                </StackPanel>
            </Grid>

            <!-- 2. CPU & GPU Section -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- CPU Card -->
                <Border Grid.Column="0" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="10">
                    <StackPanel>
                        <Grid>
                            <TextBlock Text="CPU" FontSize="11" FontWeight="Bold" Foreground="{StaticResource CpuAccent}" />
                            <TextBlock x:Name="TxtCpuPercent" Text="0%" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right" />
                        </Grid>
                        <controls:SparklineControl x:Name="CpuSparkline" Height="38" Margin="0,6,0,0" StrokeBrush="{StaticResource CpuAccent}" />
                    </StackPanel>
                </Border>

                <!-- GPU Card -->
                <Border Grid.Column="2" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="10">
                    <StackPanel>
                        <Grid>
                            <TextBlock Text="GPU" FontSize="11" FontWeight="Bold" Foreground="{StaticResource GpuAccent}" />
                            <TextBlock x:Name="TxtGpuPercent" Text="0%" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right" />
                        </Grid>
                        <controls:SparklineControl x:Name="GpuSparkline" Height="38" Margin="0,6,0,0" StrokeBrush="{StaticResource GpuAccent}" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- 3. RAM & Network Section -->
            <Grid Grid.Row="2" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- RAM Card -->
                <Border Grid.Column="0" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="10">
                    <StackPanel>
                        <Grid>
                            <TextBlock Text="MEMORY" FontSize="10" FontWeight="Bold" Foreground="{StaticResource RamAccent}" />
                            <TextBlock x:Name="TxtRamPercent" Text="0%" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right" />
                        </Grid>
                        <ProgressBar x:Name="BarRam" Height="4" Margin="0,6,0,4" Background="#333333" Foreground="{StaticResource RamAccent}" BorderThickness="0" Value="0" Maximum="100" />
                        <TextBlock x:Name="TxtRamDetail" Text="0 / 0 GB" FontSize="10" Foreground="{StaticResource TextSecondary}" />
                    </StackPanel>
                </Border>

                <!-- Network Card -->
                <Border Grid.Column="2" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="10">
                    <StackPanel>
                        <Grid>
                            <TextBlock Text="NETWORK" FontSize="10" FontWeight="Bold" Foreground="{StaticResource NetAccent}" />
                            <TextBlock x:Name="TxtNetSpeed" Text="0 KB/s" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right" />
                        </Grid>
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBlock Text="↓ " FontSize="10" Foreground="{StaticResource NetAccent}" />
                            <TextBlock x:Name="TxtNetDown" Text="0 KB/s" FontSize="10" Foreground="{StaticResource TextSecondary}" Margin="0,0,8,0" />
                            <TextBlock Text="↑ " FontSize="10" Foreground="{StaticResource NetAccent}" />
                            <TextBlock x:Name="TxtNetUp" Text="0 KB/s" FontSize="10" Foreground="{StaticResource TextSecondary}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- 4. Top Memory Hogs (Leaderboard) -->
            <Border Grid.Row="3" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="TOP MEMORY CONSUMERS" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,6" />

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl x:Name="ListTopProcesses">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2,0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="11" Foreground="{StaticResource TextPrimary}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1" Text="{Binding MemoryDisplay}" FontSize="11" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="6,0,8,0" />
                                        <Button Grid.Column="2" Content="✕" Width="20" Height="20" Style="{StaticResource DangerButton}" ToolTip="End Process" Tag="{Binding Id}" Click="BtnKillProcess_Click" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- 5. Footer / Settings -->
            <Grid Grid.Row="4" Margin="0,2,0,0">
                <CheckBox x:Name="ChkAutoStart" Content="Run on Startup" FontSize="11" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Checked="ChkAutoStart_Changed" Unchecked="ChkAutoStart_Changed" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="GitHub" Style="{StaticResource FluentButton}" Margin="0,0,6,0" Click="BtnGithub_Click" />
                    <Button Content="Quit" Style="{StaticResource DangerButton}" Click="BtnQuit_Click" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
